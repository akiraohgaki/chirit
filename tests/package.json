{
  "name": "tests",
  "version": "0.0.0",
  "description": "tests",
  "scripts": {
    "prestart": "cp ../mod.bundle.js app/",
    "start": "cd app && deno run --allow-net --allow-read server.ts",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.1",
    "@types/node": "^20.11.5"
  }
}
